/* V44 audio polish: non-invasive envelope for HTMLAudio rain-stick sounds. */
(()=>{const FADE=220,MAX=.72;function soften(a){if(!a||a.dataset.v44Soft)return;a.dataset.v44Soft='1';const wanted=Number.isFinite(a.volume)?Math.min(a.volume,MAX):MAX;a.volume=0;const fade=(from,to,ms)=>{const t0=performance.now();const tick=t=>{const p=Math.min(1,(t-t0)/ms);a.volume=Math.max(0,Math.min(1,from+(to-from)*p));if(p<1&&!a.paused)requestAnimationFrame(tick)};requestAnimationFrame(tick)};a.addEventListener('play',()=>fade(0,wanted,FADE));a.addEventListener('timeupdate',()=>{if(Number.isFinite(a.duration)&&a.duration-a.currentTime<.28&&a.volume>.08)fade(a.volume,0,FADE)})}const scan=()=>document.querySelectorAll('audio').forEach(soften);new MutationObserver(scan).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('DOMContentLoaded',scan);scan()})();