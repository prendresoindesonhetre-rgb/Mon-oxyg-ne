name: Build Chrono Meditation APK

on:
  push:
    branches:
      - chrono-meditation-android
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android 35
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Apply V10 player fixes
        working-directory: ChronoMeditation
        run: python3 v10_patch.py

      - name: Verify V10 source
        working-directory: ChronoMeditation
        run: |
          SRC=app/src/main/java/fr/prendresoindesonhetre/chronomeditation/MainActivity.java
          grep -F 'public void onBackPressed()' "$SRC"
          grep -F 'new LinearLayout.LayoutParams(MATCH, dp(220))' "$SRC"
          grep -F 'pendingCommands' "$SRC"
          grep -F 'request.isForMainFrame()' "$SRC"
          grep -F 'queueCommand(' "$SRC"

      - name: Build debug APK
        working-directory: ChronoMeditation
        run: gradle assembleDebug --no-daemon

      - name: Verify V10 launcher
        working-directory: ChronoMeditation
        run: |
          APK="app/build/outputs/apk/debug/app-debug.apk"
          "$ANDROID_HOME/build-tools/35.0.0/aapt" dump badging "$APK" | tee /tmp/badging.txt
          grep -F "package: name='fr.prendresoindesonhetre.meditationshetre.v10'" /tmp/badging.txt
          grep -F "launchable-activity: name='fr.prendresoindesonhetre.chronomeditation.MainActivity'" /tmp/badging.txt

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Launch and back-button smoke test on Android
        uses: ReactiveCircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          target: google_apis
          profile: pixel_6
          disable-animations: true
          emulator-options: -no-window -no-snapshot -noaudio -no-boot-anim -gpu swiftshader_indirect
          script: |
            adb install -r ChronoMeditation/app/build/outputs/apk/debug/app-debug.apk
            adb logcat -c
            adb shell am force-stop fr.prendresoindesonhetre.meditationshetre.v10
            adb shell am start -W -n fr.prendresoindesonhetre.meditationshetre.v10/fr.prendresoindesonhetre.chronomeditation.MainActivity | tee /tmp/start.txt
            grep -F "Status: ok" /tmp/start.txt
            sleep 4
            adb shell dumpsys activity activities | grep -F "topResumedActivity" | grep -F "fr.prendresoindesonhetre.chronomeditation.MainActivity"
            adb shell input keyevent 4
            sleep 2
            adb shell dumpsys activity activities | grep -F "topResumedActivity" | grep -F "fr.prendresoindesonhetre.chronomeditation.MainActivity"
            ! adb logcat -d | grep -A 20 'FATAL EXCEPTION' | grep -F 'fr.prendresoindesonhetre.meditationshetre.v10'

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: MesMeditations-Hetre-V10-tested
          path: ChronoMeditation/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
